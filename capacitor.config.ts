import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.kodykreskowe',
  appName: 'Kody Kreskowe',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
